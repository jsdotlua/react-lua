return {
	utils = require("./utils"),
	store = require("./store"),
	cache = require("./cache"),
	devtools = {
		Components = {
			views = {
				types = require("./views/Components/types"),
			},
		},
	},
}
